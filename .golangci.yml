version: "2"

linters:
  default: standard
  disable:
    - errcheck

  exclusions:
    rules:
      - linters:
          - staticcheck
        text: "SA1019"
        path: "services/evaluation-service"
